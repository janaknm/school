#ifndef TELEPORT_TRAP_H
#define TELEPORT_TRAP_H

//trap that random teleports player within the level
class teleport_trap : public floor {

public:
  teleport_trap();

  void step_on(animate *obj);   //if animate is player, teleport to random position in the current level

};

#endif
