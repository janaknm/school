#ifndef INANIMATE_H
#define INANIMATE_H

#include "drawable.h"

class player;

class inanimate : public drawable {
public:
  inanimate();
  ~inanimate() {}

  virtual bool be_obtained(player *p) = 0;   //pure virtual. what happens when player picks this up
  
};


#endif
