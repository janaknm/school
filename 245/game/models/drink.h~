#ifndef DRINK_H
#define DRINK_H

#include "inventory_item.h"
#include "player.h"

class drink : public inventory_item {

public:
  drink();

private:
  bool _is_health;

public:
  void use(player *p);
  
};

#endif
